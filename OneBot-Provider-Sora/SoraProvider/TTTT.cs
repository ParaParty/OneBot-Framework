using OneBot.CommandRoute.Model;
using Sora.EventArgs.SoraEvent;

namespace OneBot.Provider.SoraProvider;

public class TTTT
{
    void Tttt(OneBot.CommandRoute.Model.GroupMessage t)
    {
        // t.UserId
        if (t is UnderlayModel<GroupMessageEventArgs> t1)
        {
            t1.UnderlayModel.
        }
        
        
    }
}
